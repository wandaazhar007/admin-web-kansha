export type SelectedType = {
  selected: number
}

export type ModalEditType = {
  closeModal: () => void,
  propId: number
}

export type CategoryMapType = {
  id: number,
  name: string,
  slug: string,
  createdAt: string
}